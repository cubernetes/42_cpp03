#include <iostream>

#include "ClapTrap.hpp"

int main() {
	ClapTrap bob("Bob");
	trap.attack("Sara");
	for (int i = 0; i < 2; ++i)
		trap.takeDamage(3);
	trap.beRepaired(10);
	for (int i = 0; i < 7; ++i)
		trap.takeDamage(3);
	trap.beRepaired(10);
	return 0;
}
